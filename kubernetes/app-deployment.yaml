apiVersion: apps/v1
kind: Deployment
metadata:
  name: app-deploymnet
  namespace: dbheemu
  labels:
    app: app
spec:
  replicas: 1
  strategy:
    type: Recreate
  minReadySeconds: 20
  selector:
    matchLabels:
      app: app
  template:
    metadata:
      labels:
        app: app
    spec:
      containers:
      - envFrom:
        - secretRef:
            name: db-secret
        name: app-deployment
        image: harbor.dev.afsmtddso.com/dbheemu-harbor-project/app:e4c664b-b030b7e
        ports:
        - containerPort: 5000
